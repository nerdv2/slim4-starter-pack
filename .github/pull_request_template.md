## Summary

<!-- What changed and why. Link the related issue, if any. -->

## Checklist

- [ ] `composer run check` passes (composer validate + PHPStan + PHPCS)
- [ ] `composer run test` passes
- [ ] `php -l` passes for every changed PHP file
- [ ] OpenAPI docs regenerated and routes cached when endpoints or routes changed
- [ ] No secrets, credentials or `.env` values are included
- [ ] `README.md`, `AGENTS.md` and `docs/` updated when behaviour or conventions change
