# Uploaded files are user-controlled data and must never be executable.
location ~* ^/uploads/.*\.(?:php[0-9]?|phtml|phar|cgi|pl|py|sh)(?:$|/) {
    return 404;
}

location ~* ^/uploads/ {
    add_header X-Content-Type-Options "nosniff" always;
}
